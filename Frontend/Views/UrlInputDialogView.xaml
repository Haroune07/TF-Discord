<Window x:Class="Frontend.Views.UrlInputWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Frontend.Views"
        mc:Ignorable="d"
        Title="Saisie de l'URL"
        Height="150" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <StackPanel Margin="10" VerticalAlignment="Center">
        <TextBlock Text="Entrez l'URL de l'image :" Margin="0,0,0,10"/>
        <TextBox x:Name="UrlTextBox" Width="300" Height="25"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" Width="60" Click="OkButton_Click" IsDefault="True" Margin="0,0,10,0"/>
            <Button Content="Annuler" Width="60" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
