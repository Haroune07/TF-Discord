<UserControl x:Class="Frontend.Views.ServerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:Frontend.Views"
             mc:Ignorable="d"
             d:DesignHeight="350"
             Width="72">

    <StackPanel>

        <ItemsControl ItemsSource="{Binding Servers}">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>

                    <views:ServerView
                        Margin="0,5"
                        Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ItemsControl}}">

                        <views:ServerView.ContextMenu>

                            <ContextMenu>

                                <MenuItem Header="Quitter le serveur"
                                          Command="{Binding PlacementTarget.Tag.LeaveServerCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />

                                <MenuItem Header="Copier l'ID du serveur"
                                          Command="{Binding PlacementTarget.Tag.CopyServerIdCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />

                            </ContextMenu>

                        </views:ServerView.ContextMenu>

                    </views:ServerView>

                </DataTemplate>
            </ItemsControl.ItemTemplate>

        </ItemsControl>

        <Button Content="+"
                Command="{Binding CreateServerCommand}"
                Width="48"
                Height="48"
                Background="#3BA55D"
                Foreground="White"
                FontSize="24"
                FontWeight="Bold"
                BorderThickness="0"
                Margin="0,8,0,0"
                Cursor="Hand">

            <Button.Style>

                <Style TargetType="Button">

                    <Setter Property="Background" Value="#313338"/>
                    <Setter Property="Foreground" Value="#3BA55D"/>
                    <Setter Property="BorderThickness" Value="0"/>

                    <Style.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#3A3C43"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>

                    </Style.Triggers>

                </Style>

            </Button.Style>

            <Button.Resources>

                <Style TargetType="Border">

                    <Setter Property="CornerRadius" Value="24"/>

                </Style>

            </Button.Resources>

        </Button>

    </StackPanel>

</UserControl>