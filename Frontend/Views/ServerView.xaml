<UserControl x:Class="Frontend.Views.ServerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Frontend.Views"
             mc:Ignorable="d" 
             d:DesignHeight="48" d:DesignWidth="48">
    <Border CornerRadius="14" Width="48" Height="48" Background="#5865F2" ClipToBounds="True">
        <Button Command="{Binding Test}" Background="Transparent" BorderThickness="0">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid Background="Transparent">
                        <Image Source="/Static/Images/bg_auth.jpg" />
                    </Grid>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Border>
</UserControl>
