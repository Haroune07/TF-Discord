<UserControl x:Class="Frontend.Views.ChannelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Frontend.Views"
             mc:Ignorable="d" Height="30" Width="230">
    <Border CornerRadius="5" Background="#232428">
        <Button Command="{Binding SelectCommand}" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left" Padding="5">
            <TextBlock Text="{Binding Name, StringFormat='# : {0}'}" Foreground="White" />
        </Button>
    </Border>
</UserControl>
